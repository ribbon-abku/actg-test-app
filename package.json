{
  "name": "actg-test-app",
  "version": "1.0.0",
  "description": "An API testing app to test cross-domain requests via CORS",
  "main": "index.js",
  "scripts": {
    "serve": "npx avityam -d=./ -p=9090",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Abhishek Kumar",
  "license": "ISC",
  "devDependencies": {
    "avityam": "^1.0.2"
  }
}
